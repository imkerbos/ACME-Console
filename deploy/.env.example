# =============================================================================
# ACME Console — Production Environment Variables
# =============================================================================
# Copy this file to .env and adjust values for your environment.
#   cp deploy/.env.example deploy/.env
#
# Also create your production config:
#   cp configs/config.example.yaml deploy/configs/config.yaml
#   # Edit deploy/configs/config.yaml with production values

# --- MySQL -------------------------------------------------------------------
MYSQL_ROOT_PASSWORD=change_me_root
MYSQL_DATABASE=acme_console
MYSQL_USER=acme
MYSQL_PASSWORD=change_me
MYSQL_PORT=3306

# --- Backend (Go API) -------------------------------------------------------
API_PORT=10020

# --- Frontend (Nginx) -------------------------------------------------------
WEB_PORT=80
