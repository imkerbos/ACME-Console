server:
  host: "0.0.0.0"
  port: 10020

database:
  host: "localhost"
  port: 3306
  user: "acme"
  password: "change_me"
  dbname: "acme_console"

jwt:
  secret: "change-me-to-a-random-secret"
  expire_hours: 24

# ACME Configuration
acme:
  dns:
    resolvers: "8.8.8.8:53,1.1.1.1:53"
    timeout: "10s"

# Encryption Configuration
# Required for storing private keys securely
encryption:
  master_key: ""  # 32-byte hex-encoded key (64 characters). Generate with: openssl rand -hex 32
